function startGame(slotId) {
  alert(`正在載入存檔 ${slotId}，冒險即將開始...`);
  // 實作後續跳轉到實際遊戲場景 (例如: window.location.href = '../game/index.html')
}

function goBack() {
  window.location.href = '../index.html';
}
